# 增加栈大小以支持编译大文件（如 codegen_s.xy 190KB）
# 默认 Windows 主线程栈为 1MB，大型递归下降解析器需要更大栈空间
[target.'cfg(windows)']
rustflags = ["-C", "link-arg=/STACK:8388608"]

[target.'cfg(not(windows))']
rustflags = ["-C", "link-arg=-Wl,-z,stack-size=8388608"]
