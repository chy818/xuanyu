/**
 * @file types.xy
 * @brief 玄语L2编译器 - 共享类型定义
 * @description 所有模块共享的类型、
/* ============ Token 结构 ============ */
/* ============ AST 节点类型 ============ */
/* ============ AST 节点
/* ============ Lexer 
/* ============ Lexer 上下文
/* ============ Parser 
/* ============ 类型别名
/* ============ 符号表
/* ============ 作用域
/* ============ 循环状态
/* ============ Sema 
/* ============ 循环上下文
/* ============ Codegen 
/* ============ 错误级别
/* ============ 错误恢复

函数 主(): 整数 { 返回 0 }
