/**
 * @file test_import.xy
 * @brief 测试模块导入功能
 */

引入 "examples/mod_test.xy"

函数 主(): 整数 {
    定义 result = add(10, 5)
    打印("10 + 5 = ")
    打印(result)
    
    定义 diff = sub(10, 5)
    打印("10 - 5 = ")
    打印(diff)
    
    返回 0
}
