/**
 * @file test_enum.xy
 * @brief 枚举和模式匹配测试
 */

// 简单枚举定义
枚举 颜色 { 红, 绿, 蓝 }

// 带字段的枚举
枚举 节点 {
    值(整数),
    加(整数, 整数)
}

/**
 * 测试函数
 */
函数 主 函数(): 整数 {
    定义 c = 红
    打印("测试枚举")

    定义 n = 值(42)
    打印("测试完成")

    返回 0
}
