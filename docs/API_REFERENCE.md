# 玄语语言 API 参考文档

本文档提供玄语编程语言的完整 API 参考。

## 目录

- [内置类型](#内置类型)
- [关键字](#关键字)
- [运算符](#运算符)
- [内置函数](#内置函数)
- [标准库](#标准库)

---

## 内置类型

### 基本类型

| 类型 | 中文名 | 描述 | 示例 |
|------|--------|------|------|
| `Int` | 整数 | 有符号整型（LLVM 中为 i64） | `42`, `-10`, `0` |
| `Long` | 长整数 | 有符号整型（与整数同宽，LLVM `i64`） | `9223372036854775807` |
| `Float` | 浮点数 | 浮点类型（LLVM 中为 `double`） | `3.14`, `-0.5` |
| `Double` | 双精度 | 浮点类型（LLVM `double`，与浮点同宽） | `3.14159265358979` |
| `Bool` | 布尔 | 布尔值 | `真`, `假`, `true`, `false` |
| `String` | 文本 | UTF-8 编码字符串 | `"你好"`, `"Hello"` |
| `Char` | 字符 | Unicode 字符 | `'a'` |
| `Void` | 无返回 | 无返回值的函数 | 用于函数返回类型 |
| `Pointer` | 指针 | 内存指针 | `i8*`, `ptr` |

> 说明：玄语的 `整数`/`长整数` 均映射为 LLVM `i64`，`浮点数`/`双精度` 均映射为 LLVM `double`（见 `src/codegen/codegen.rs`）。

### 复合类型

| 类型 | 语法 | 描述 |
|------|------|------|
| 列表 | `List<T>` / `列表` | 动态列表 |
| 数组 | `T[]` | 元素类型为 T 的数组 |
| 或许 | `Optional<T>` / `或许` | 可能为空的值 |
| 函数 | `函数 (T1, T2) -> R` | 函数类型 |
| 指针 | `指针<T>` | 元素类型为 T 的指针 |

---

## 关键字

### 控制流

| 中文关键字 | 英文别名 | 描述 |
|------------|----------|------|
| `若` | `if` | 条件判断 |
| `否则` | `else` | 否则分支 |
| `否则若` | `elif` | 否则若分支 |
| `则` | `then` | 条件成立时执行 |
| `当` | `while` | 条件循环 |
| `直到` | `until` | 直到循环 |
| `循环` | `loop` | 循环（无限循环/计数循环/遍历） |
| `从` | `from` | 计数循环起始（`循环 从 i 到 10`） |
| `到` | `to` | 计数循环结束 |
| `取自` | `in` | 遍历集合 |
| `退出` | `break` | 跳出循环 |
| `跳过` | `continue` | 跳过本次循环 |
| `返回` | `return` | 返回语句 |

### 模式匹配 (v0.3.0)

| 中文关键字 | 英文别名 | 描述 |
|------------|----------|------|
| `匹配` | `match` | 模式匹配入口 |
| `情况` | `case` | 匹配分支 |
| `默认` | `default` | 默认（通配）分支 |

### 异步编程 (v0.3.0)

| 中文关键字 | 英文别名 | 描述 |
|------------|----------|------|
| `异步` | `async` | 声明异步函数 |
| `等待` | `await` | 等待协程完成并获取结果 |
| `启动` | `spawn` | 启动异步协程任务 |

### 定义

| 中文关键字 | 英文别名 | 描述 |
|------------|----------|------|
| `函数` | `fn` | 定义函数 |
| `过程` | `proc` | 定义过程 |
| `定义` | `let` | 定义变量 |
| `可变` | `mut` | 可变变量修饰符 |
| `常量` | `const` | 定义常量 |
| `结构体` | `struct` | 定义结构体 |
| `枚举` | `enum` | 定义枚举 |
| `类型` | `type` | 类型别名 |

### 模块

| 中文关键字 | 英文别名 | 描述 |
|------------|----------|------|
| `引入` | `import` / `use` | 导入模块 |
| `导出` | `export` / `pub` | 导出符号 |
| `外部` | `extern` | 外部函数声明 |
| `模块` | `module` | 声明模块 |

---

## 运算符

### 算术运算符

| 运算符 | 描述 | 示例 |
|--------|------|------|
| `+` | 加法 | `a + b` |
| `-` | 减法 | `a - b` |
| `*` | 乘法 | `a * b` |
| `/` | 除法 | `a / b` |
| `%` | 取模 | `a % b` |

### 比较运算符

| 运算符 | 描述 | 示例 |
|--------|------|------|
| `==` | 等于 | `a == b` |
| `!=` | 不等于 | `a != b` |
| `<` | 小于 | `a < b` |
| `>` | 大于 | `a > b` |
| `<=` | 小于等于 | `a <= b` |
| `>=` | 大于等于 | `a >= b` |

### 逻辑运算符

| 运算符 | 描述 | 示例 |
|--------|------|------|
| `&&` | 逻辑与 | `a && b` |
| `\|\|` | 逻辑或 | `a \|\| b` |
| `!` | 逻辑非 | `!a` |

### 位运算符

| 运算符 | 描述 | 示例 |
|--------|------|------|
| `&` | 按位与 | `a & b` |
| `\|` | 按位或 | `a \| b` |
| `^` | 按位异或 | `a ^ b` |
| `~` | 按位取反 | `~a` |
| `<<` | 左移 | `a << n` |
| `>>` | 右移 | `a >> n` |

### 赋值运算符

| 运算符 | 描述 | 示例 |
|--------|------|------|
| `=` | 赋值 | `a = b` |
| `+=` | 加法赋值 | `a += b` |
| `-=` | 减法赋值 | `a -= b` |
| `*=` | 乘法赋值 | `a *= b` |
| `/=` | 除法赋值 | `a /= b` |

---

## 模式匹配 (v0.3.0)

模式匹配语句用于对枚举变体进行分支判断，支持字段绑定和默认分支。

### 语法

```xy
匹配 表达式 {
    情况 变体1 => { 分支体 }
    情况 变体2(字段名) => { 分支体 }  // 字段绑定
    默认 => { 默认分支 }
}
```

### 示例

```xy
枚举 颜色 { 红, 绿, 蓝 }

函数 主(): 整数 {
    定义 颜色值 = 红
    定义 可变 结果: 整数 = 0

    匹配 颜色值 {
        情况 红 => { 打印("红色") 结果 = 1 }
        情况 绿 => { 打印("绿色") 结果 = 2 }
        情况 蓝 => { 打印("蓝色") 结果 = 3 }
        默认 => { 打印("未知") 结果 = 0 }
    }
    返回 结果
}
```

### 说明

- 判别方式：枚举变体通过 `icmp eq` 比较判别值 (i64)
- 默认分支：`默认`/`default` 分支在所有显式分支未命中时执行
- 穷尽性：编译器建议覆盖所有变体，否则使用默认分支兜底

---

## 异步编程 (v0.3.0)

玄语支持基于轻量协程的异步编程模型。`异步` 函数通过协程包装器适配 runtime ABI，`启动` 将函数注册为协程任务，`等待` 等待任务完成并获取结果。

### 语法

```xy
异步 函数 异步任务(参数: 类型): 返回类型 {
    // 异步函数体
    返回 结果
}

定义 句柄 = 启动 异步任务(实参)    // → i64 协程句柄
定义 结果 = 等待 句柄                // → 协程返回值
```

### 示例

```xy
异步 函数 计算(次数: 整数): 整数 {
    定义 可变 总和 = 0
    循环 从 i 到 次数 {
        总和 = 总和 + i
    }
    返回 总和
}

函数 主(): 整数 {
    定义 任务 = 启动 计算(100)
    定义 结果 = 等待 任务
    打印整数(结果)  // 输出: 5050
    返回 0
}
```

### 说明

- **协程模型**：run-to-completion，协程在首次 `等待` 时执行到完成
- **ABI**：协程包装器统一为 `i64(i64)` 签名，通过参数结构体传参
- **Runtime 函数**：`rt_coro_spawn`/`rt_coro_await`/`rt_coro_run_all`
- **限制**：`启动` 只能用于 `异步` 声明的函数

---

## 调试器 (v0.3.0)

使用 `--debug` 标志启用运行时调试支持。

### 命令

| 命令 | 别名 | 功能 |
|------|------|------|
| `s` / `单步` | `step` | 单步执行下一条语句 |
| `c` / `继续` | `continue` | 继续执行到下一个断点 |
| `q` / `退出` | `quit` | 退出程序 |
| `h` / `帮助` | `help` | 显示帮助信息 |

### 使用方式

```bash
# 编译并进入调试模式
xy program.xy --debug --build
./output.exe

# 在 (xy-dbg) 提示符下输入命令
```

---

## 内置函数

### 输入输出

#### `打印整数(数值: 整数)`

打印整数到标准输出。

**参数:**
- `数值`: 要打印的整数

**示例:**
```xy
打印整数(42)  // 输出: 42
```

---

#### `打印浮点(数值: 浮点数)`

打印浮点数到标准输出。

**参数:**
- `数值`: 要打印的浮点数

**示例:**
```xy
打印浮点(3.14)  // 输出: 3.14
```

---

#### `打印(text: 文本)`

打印文本到标准输出（不换行）。

**参数:**
- `text`: 要打印的字符串

**示例:**
```xy
打印("你好，玄语！")  // 输出: 你好，玄语！
```

---

#### `输入整数(): 整数`

从标准输入读取一个整数。

**返回值:**
- 读取的整数

**示例:**
```xy
定义 数值 = 输入整数()
打印整数(数值)
```

---

#### `输入文本(): 文本`

从标准输入读取一行文本。

**返回值:**
- 读取的文本

**示例:**
```xy
定义 名字 = 输入文本()
打印(名字)
```

---

## 标准库

### string 模块

#### `文本长度(文本: 文本): 整数`

计算字符串长度（内置函数）。

**参数:**
- `文本`: 输入字符串

**返回值:**
- 字符串长度

---

#### `文本拼接(甲: 文本, 乙: 文本): 文本`

拼接两个字符串（内置函数）。

**参数:**
- `甲`: 第一个字符串
- `乙`: 第二个字符串

**返回值:**
- 拼接后的字符串

---

此外 `std/string.xy` 还提供了 `字符串长度`、`字符串拼接`、`字符串复制` 等外部函数声明。

### list / array 模块

#### `创建列表(): 列表`

创建空列表。

---

#### `列表添加(列表: 列表, 元素: Any): 整数`

向列表追加元素。

---

#### `列表获取(列表: 列表, 索引: 整数): Any`

获取索引处的元素。

---

#### `列表长度(列表: 列表): 整数`

获取列表长度。

---

### 文本转换

#### `文本转整数(文本: 文本): 整数`

将字符串转换为整数。

---

#### `整数转文本(整数: 整数): 文本`

将整数转换为字符串。

---

#### `文本包含(文本: 文本, 子串: 文本): 整数`

判断字符串是否包含子串。

---

#### `文本切片(文本: 文本, 开始: 整数, 结束: 整数): 文本`

截取字符串从 `开始` 到 `结束` 的子串。

---

### math 模块

#### `绝对值(数值: 整数): 整数`

计算整数的绝对值（`std/math.xy`）。

**参数:**
- `数值`: 输入整数

**返回值:**
- 绝对值

---

### IO / 文件

#### `文件读取(路径: 文本): 文本`

读取文件全部内容。

---

#### `文件写入(路径: 文本, 内容: 文本): 整数`

写入文件。

---

#### `文件存在(路径: 文本): 整数`

判断文件是否存在。

---

#### `执行命令(命令: 文本): 整数`

执行系统命令。

---

#### `命令输出(命令: 文本): 文本`

执行命令并返回输出。

---

#### `参数个数(): 整数`

返回命令行参数个数。

---

#### `获取参数(索引: 整数): 文本`

获取第 `索引` 个命令行参数。

---

## 错误码

| 错误码 | 描述 |
|--------|------|
| `CCAS-T001` | 未定义的变量 |
| `CCAS-T002` | 不可变变量重新赋值 |
| `CCAS-T003` | 赋值类型不匹配 |
| `CCAS-P001` | 语法错误 |
| `CCAS-L001` | 词法错误 |

---

## 版本信息

- 文档版本: v0.3.0-beta
- 语言版本: v0.3.0-beta
- 最后更新: 2026-08-09
